name : 05 - 1 - Event Filters and Activity Types

on: 
    pull_request: 
        types: 
            - opened
            - synchronize
        branches:
            - main  

jobs: 
    echo :
        runs-on : ubuntu-latest
        steps : 
            - name : Echoing message
              run : echo "This job runs only on pull_request event when activity type is opened or synchronize and base branch is main"