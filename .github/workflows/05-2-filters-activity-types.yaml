name : 05 - 2 - Event Filters and Activity Types

on: 
    pull_request:
        types: 
            - closed
        branches:
            - main

jobs: 
    echo :
        runs-on : ubuntu-latest
        steps : 
            - name : Echoing message
              run : echo "This job runs only on pull_request event when activity type is closed and