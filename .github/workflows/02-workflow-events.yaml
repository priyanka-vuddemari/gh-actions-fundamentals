name: 02 - Workflow Events

on: push
jobs : 
    echo:
      runs-on : unbuntu-latest
      steps :
        - name : Show the event that triggered this workflow
          run  : cat $GITHUB_EVENT_PATH